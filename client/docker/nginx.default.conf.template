server {
    listen ${PORT};
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # API calls use REACT_APP_API_BASE_URL at build time (direct to backend).
    # SPA fallback
    location / {
        try_files $uri /index.html;
    }
}
